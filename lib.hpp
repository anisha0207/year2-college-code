#pragma once
#include <unordered_map>
#include <string>
#include <vector>

// Adjust types as appropriate for your code (only implemented in server.cpp)
bool save_storage_to_disk(const HashMap& storage, const std::string& filename);
